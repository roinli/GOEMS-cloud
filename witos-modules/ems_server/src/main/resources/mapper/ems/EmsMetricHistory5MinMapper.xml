<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.witos.ems.server.mapper.EmsMetricHistory5MinMapper">

    <insert id="upsert">
        insert into ems_metric_history_5min
        (tenant_id, company_id, station_id, device_id, device_component_id, server_endpoint_id,
         edge_id, component_id, serial_no, metric_key, report_method, source_role, source_priority,
         bucket_time, metric_value, metric_text, unit, quality, quality_reason, source_sample_time,
         create_by, create_time, update_by, update_time, remark)
        values
        (#{row.tenantId}, #{row.companyId}, #{row.stationId}, #{row.deviceId}, #{row.deviceComponentId},
         #{row.serverEndpointId}, #{row.edgeId}, #{row.componentId}, #{row.serialNo}, #{row.metricKey},
         #{row.reportMethod}, #{row.sourceRole}, #{row.sourcePriority}, #{row.bucketTime}, #{row.metricValue},
         #{row.metricText}, #{row.unit}, #{row.quality}, #{row.qualityReason}, #{row.sourceSampleTime},
         #{row.createBy}, #{row.createTime}, #{row.updateBy}, #{row.updateTime}, #{row.remark})
        on duplicate key update
            company_id = values(company_id),
            station_id = values(station_id),
            device_component_id = values(device_component_id),
            server_endpoint_id = values(server_endpoint_id),
            edge_id = values(edge_id),
            component_id = values(component_id),
            serial_no = values(serial_no),
            report_method = values(report_method),
            source_role = values(source_role),
            source_priority = values(source_priority),
            metric_value = values(metric_value),
            metric_text = values(metric_text),
            unit = values(unit),
            quality = values(quality),
            quality_reason = values(quality_reason),
            source_sample_time = values(source_sample_time),
            update_by = values(update_by),
            update_time = values(update_time),
            remark = values(remark)
    </insert>

    <select id="selectCoverage" resultType="java.util.LinkedHashMap">
        select device_id as deviceId,
               metric_key as metricKey,
               bucket_time as bucketTime,
               quality
        from ems_metric_history_5min
        where tenant_id = #{tenantId}
          and bucket_time &gt;= #{startTime}
          and bucket_time &lt; #{endTime}
    </select>

</mapper>
